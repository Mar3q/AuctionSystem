{% extends 'base.html.twig' %}

{% block page_content %}
    <h1>Szczegóły</h1>
    {{ auction.title }}
    {{ auction.description }}
    {{ auction.price }}

    <a href="{{ path('auction_index') }}">Powrót</a>
{% endblock %}